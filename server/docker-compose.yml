version: '3.8'

services:
  postgresdb:
    image: postgres
    ports:
      - 5432:5432
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: todo-app
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
